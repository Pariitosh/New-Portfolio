/* eslint-disable no-unused-vars */
/* eslint-disable react/prop-types */
/* eslint-disable react/no-unknown-property */
import { Bloom, EffectComposer, SMAA } from "@react-three/postprocessing";
import { Text, Sphere, Box, MeshReflectorMaterial, Text3D, useDetectGPU, PerformanceMonitor, RoundedBox } from "@react-three/drei";
import { useEffect, useState, useRef } from "react";
import gsap from "gsap";
export default function Balls({ Lights, row1, row2, row3, row4, row5, row6, row7, row8, row9, row10, row11, row12, row13, row14, row15, row16, row17, row18, row19, row20, row21
      , row22, row23, row26, row27, row28 }) {

      const GPUTier = useDetectGPU()

      const sphere28 = useRef()
      const sphere29 = useRef()
      
      return (
            <>
                  


            </>
      )
}